# Usage

Current command surface:

```sh
compactor --help
compactor --version
```

Planned command areas:

- Inspect supported agent surfaces for Claude and Codex.
- Convert compaction input into document shards.
- Write a compact reference index.
- Resolve a reference back to its source document.
- Validate generated documents for drift, missing references, and unsafe paths.

Command names and flags should stay unsettled until the Claude and Codex research is complete.

